<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.demo2.Mapper.ForgetMapper">

    <insert id="forgetAccount" parameterType="org.example.demo2.dto.Account" useGeneratedKeys="true" keyProperty="id">
        update scorelogin set password=(#{account.password}) where username=(#{account.username})
    </insert>

    <select id="findName1" resultType="java.lang.String">
        select name from scorelogin where username=#{username}
    </select>
</mapper>
